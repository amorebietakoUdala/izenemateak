{% extends 'base.html.twig' %}

{% block title %}{{ 'title.course.list' | trans }}{% endblock %}

{% block body %}
	<main class="container-fluid">
        <div class="row">
            <div class="col">
                {{ include('course/_searchForm.html.twig') }}
            </div>
        </div>
        {{ include('course/_list.html.twig') }}
        <span class="float-right">
            <a href="{{ path ('app_course_new') }}" class="btn btn-primary rounded-circle fixed-bottom-right">
                <i class="fa fa-plus" title='{{ "btn.new" | trans | capitalize }}'></i>
            </a>
        </span>
	</main>
{% endblock body %}