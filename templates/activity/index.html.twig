{% extends 'base.html.twig' %}

{% block title %}{{ 'title.activity.list' | trans }}{% endblock %}

{% block body %}
	<main class="container-fluid">
        <div class="row">
            <div class="col">
                {{ include('activity/_searchForm.html.twig') }}
            </div>
        </div>
        {{ include('activity/_list.html.twig') }}
        <span class="float-right">
            <a href="{{ path ('app_activity_new') }}" class="btn btn-primary rounded-circle fixed-bottom-right">
                <i class="fa fa-plus" title='{{ "btn.new" | trans | capitalize }}'></i>
            </a>
        </span>
	</main>
{% endblock body %}