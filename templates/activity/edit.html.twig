{% extends 'base.html.twig' %}

{% block title %}
    {% if new %}
        {% set title = 'title.activity.new' | trans | capitalize %}
    {% elseif readonly %}
        {% set title = 'title.activity.show' | trans | capitalize %}
    {% else %}
        {% set title = 'title.activity.edit' | trans | capitalize %}
    {% endif %}
    {{ title }}
{% endblock %}

{% block body %}
    {% if new %}
        {% set title = 'title.activity.new' | trans | capitalize %}
    {% elseif readonly %}
        {% set title = 'title.activity.show' | trans | capitalize %}
    {% else %}
        {% set title = 'title.activity.edit' | trans | capitalize %}
    {% endif %}
    <div class="d-flex align-items-center">
        <h2>{{ title }}</h2>
        {% if not new and not readonly %}
            &nbsp;<span>{{ include('activity/_delete_form.html.twig') }}</span>
        {% endif %}
    </div>

    {{ include('activity/_form.html.twig') }}

    {{ include('common/_alert.html.twig', {
        'confirmationText' : 'messages.confirmationDetail' | trans 
    }) }}

{% endblock %}
