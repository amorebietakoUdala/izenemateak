{% extends 'base.html.twig' %}

{% block title %}{{ 'title.register' | trans | capitalize }}{% endblock %}

{% block body %}
    {% if activeCourses | length > 0 %}
        {{ include('register/_form.html.twig') }}
    {% else %}
        <div class="container-fluid">
            <div class="alert alert-secondary" role="alert">{{"messages.noActiveCourses" | trans }}</div>
        </div>
    {% endif %}
{% endblock %}

