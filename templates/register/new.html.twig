{% extends 'base.html.twig' %}

{% block title %}{{ 'title.register' | trans | capitalize }}{% endblock %}

{% block body %}
    {% if activeActivitys | length > 0 %}
        {{ include('register/_form.html.twig') }}
    {% else %}
        <div class="container-fluid">
            <div class="alert alert-secondary" role="alert">{{"messages.noActiveActivitys" | trans }}</div>
        </div>
    {% endif %}
{% endblock %}

