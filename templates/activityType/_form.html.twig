{{ form_start(form) }}
    <div class="container-fluid">
        <div class="row">
            <div class="col-auto">
                {{ form_widget(form) }}
            </div>
        </div>
        <button class="btn">{{ button_label|default('btn.save' | trans ) }}</button>
    </div>
{{ form_end(form) }}
