{% extends 'base.html.twig' %}

{% block title %}{{'title.clasification.edit' | trans }}{% endblock %}

{% block body %}
    <h2>{{'title.clasification.edit' | trans }}</h2>

    {{ include('clasification/_form.html.twig', {'button_label': 'btn.save'| trans}) }}

    <a href="{{ path('app_clasification_index') }}">{{ 'btn.cancel' | trans }}</a>

    {{ include('clasification/_delete_form.html.twig') }}
{% endblock %}
